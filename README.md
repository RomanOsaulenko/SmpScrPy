# SmpScrPy
A simple asynchronous Python Lib scraper for YAML feeds. Details will be available after all tests are complete. Main goal save memory while it works

# Requirements
aiohttp, selectolax (lexbor)